# 虚空のインフェルニティ資産発掘 公開監査

状態: `[AUDIT]` `[PUBLIC-PROJECTION-AUTHORIZED]` `[ORIGINALS-READ-ONLY]`

観測時刻: `2026-07-29T14:41:14+09:00`

- Manifest source revision: `47c03a73c49940b40358537d2818017675f51b2a`
- Q Atlantis source revision: `3ffb7c543b4e02aefdf5737857725ef74fcbbd8f`
- MAGI bundle: `SphereOS-Atlantis/magi/0.2.1/bundle.json`
- canonical coordinate: `0.200.1`
- supplied source: `INFERNITY_BLENDER_PIPELINE_SALVAGE_INDEX_2026-07-27.md`
- supplied source SHA-256: `e6a938c01b904db02e1478fc39f12e33a9e431c9c196f172de2b21c0235043da`

## 1. User Gate

[FACT] Userは、読み取り専用で作成済みの発掘索引をManifestとQ Atlantisの適切な棚へ追加し、
Q Atlantisのlocalhost previewを再構成する権限を付与した。

[FACT] 原本、legacy `.blend`、原稿、外付けvolume、iCloud objectを移動、変換、実行、
一括downloadする権限は今回付与されていない。

[FACT] Q Atlantisは既存`dev`で作業し、Userのlocalhost目視と別指示まで`main` mergeと
production deployを行わない。

## 2. Declared Positionとclaim scope

Declared Position:

- 発掘物は単一の自作engineではなく、第三者生成器、ユーザー固有の選択・統合・設定・素材・
  scene構築・漫画工程を束ねた制作pipelineとして記録する
- Manifestは系譜、所管、復元Gate、現代再構成候補を所有する
- Q Atlantisは一般読者向けの公開projectionと参加入口を所有する
- 原本path、秘密、個人識別情報、電話記録、会社案件、医療情報を公開面へ移さない

Position-talk Risk:

- 第三者generatorやupstream runtimeをinfernoayase独自実装へ昇格する
- 同居、更新時刻、作品世界の連続性だけで直接sourceや作者性を確定する
- 古いassetの存在を、現在のengineが実行可能である証拠へ変換する
- 物語的な系譜を自然科学、通信性能、統計的再現性へ誤用する
- 公開価値を優先してlocal absolute path、credential、個人情報を漏らす

媒体とclaim scope:

- supplied source: 読み取り専用調査の詳細索引。公開repositoryへ原本複製しない
- Manifest: 横断的な正本候補
- Q Atlantis: 圧縮した公開棚
- legacy volume／iCloud: untouched evidence。今回の操作対象外

## 3. MAGI Position別監査

### Maxwell

[INTERPRETATION] Blender、Minecraft、KAG/TJS、マイコン、ESP32を製品名の一覧として
凍結するより、`world/scenario command -> typed event graph -> deterministic state machine
-> adapter -> provenance receipt`という共通構造へ接続すると、過去資産を現代R&Dの入口へ変えられる。

一方、未来のworld compilerを実装済みとせず、現時点では再構成仮説として掲示する。

action gate: `pass`

### Uriel

[FACT] 読み取り専用索引は、Suicidator City Generator、Blender Tree-maker、Blended Cities、
SCG v0.5.6、Blender 2.49 portable環境、Minecraft class改造、Habakiri、KAS、ONScripter等の
系譜と権利境界を分離している。

[FACT] 2013年のBlended Cities系とSCG v0.5.6系は、複数backupが同一collection hashである。
代表一組をworking copyにし、残りをuntouched evidenceへ保つ方針を採れる。

[UNKNOWN] 生成器codeへのユーザー固有改変、原稿pageと背景sourceの一対一対応、購入／共同制作
素材の再配布条件、欠落した旧versionとsourceは未確定である。

action gate: `pass with unknowns preserved`

### Raphael

[INTERPRETATION] generatorの発見、file headerの読取、render画像の視認、隔離VMでの再現、
現代engineへの再実装、公開可能性は別々のgreenである。

公開面ではstorageの絶対pathを抽象化し、source basenameとdigestだけを記録する。
会社案件、認証情報、server log、個人名、電話・医療隣接情報を混ぜない。

action gate: `pass`

## 4. agreements／disagreements／unknown

agreements:

- 旧形式を原本上で開かず、固定seedと隔離working copyで再現する
- 第三者codeの現代化はlicense確認または挙動のclean-room再実装を先行する
- 原稿と3D資産の同居は工程接続候補であり、page単位の直接source証明ではない
- `UNKNOWN`と陰性所見を発見失敗として消さない
- MIPはfilename、全文、hash、index、重複判定を含む探索の完全除外対象とする

disagreements:

- なし。三Positionは再利用価値を認めつつ、作者性、実行可能性、公開権利を分離している

[UNKNOWN]

- legacy assetの現行storage availability
- 隔離VMでの再現結果
- 各generatorのlocal patch差分
- 原稿page、render、scene、generator、素材licenseの完全な系譜
- 現代world compilerの実装repositoryと最小schema

## 5. OAE Temporal Integrity

`historical-oae-unavailable`

本監査は2026年7月29日の索引と現行repositoryから作った現在のInterpretation OAEである。
2008〜2016年の制作時点へ、現在のObserver、Agency role、Intent、製品roadmapを遡及生成しない。

## 6. Last Order

1. Manifestへ権利境界と保全Gateを持つ発掘正本を追加する
2. Q Atlantisへlocal absolute pathを除いた公開projectionを追加する
3. source basenameとSHA-256で変換元を固定する
4. 文書検証、Q build、localhost previewを実施する
5. Userの別指示まで原本操作、隔離VM再現、Qの`main` merge、production deployを行わない
